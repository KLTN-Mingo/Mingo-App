import { View } from "react-native";

/**
 * Tab giữa — chỉ dùng để căn đều 5 tab; mở tạo bài qua listener trong _layout.
 */
export default function PostTabScreen() {
  return <View className="flex-1 bg-background-light dark:bg-background-dark" />;
}
